#ifndef DATA_STRUCTURES
#define DATA_STRUCTURES

struct memorysystem {
    unsigned address; // Holds the hex value of the 
    char function; // Holds the function type (Read or Write) for a trace
};

#endif
